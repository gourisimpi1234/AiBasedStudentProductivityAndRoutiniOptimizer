
  # Create Animated Logo

  This is a code bundle for Create Animated Logo. The original project is available at https://www.figma.com/design/7sU8d1QtmTnpdcPqoPyvgT/Create-Animated-Logo.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  